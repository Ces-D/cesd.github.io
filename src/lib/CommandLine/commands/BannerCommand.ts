import type { Command } from "../definitions"

const BannerCommand: Command = {
    name: "banner",
    description: "Banner!",
    optionDefinitions: [],
    handle: () => { }
}

export default BannerCommand
